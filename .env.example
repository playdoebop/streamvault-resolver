# StreamVault Resolver — copy to .env and fill in values

# TMDB API key (v3) — get free at themoviedb.org
TMDB_API_KEY=your_tmdb_api_key

# Port (Render sets this automatically)
PORT=7000

# Log level: error | warn | info | debug
LOG_LEVEL=info
